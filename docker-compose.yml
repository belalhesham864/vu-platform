version: '3.8'

services:
  app:
    image: ghcr.io/belalhesham864/vu-platform:latest
    restart: unless-stopped
    env_file: .env
    # تم إزالة الـ Volume الذي كان يربط المجلد المحلي لضمان استخدام الكود الجديد من الصورة
    volumes:
      - app-content:/var/www
    networks:
      - vu-network
    depends_on:
      - db
      - redis

  web:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # نستخدم نفس المجلد من حاوية app لكي يستطيع Nginx الوصول للملفات العامة (public)
      - app-content:/var/www
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
    networks:
      - vu-network
    depends_on:
      - app

  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - vu-network

  redis:
    image: redis:alpine
    restart: unless-stopped
    networks:
      - vu-network

  certbot:
    image: certbot/certbot
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - app-content:/var/www
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

networks:
  vu-network:
    driver: bridge

volumes:
  app-content: # مجلد مشترك لضمان تزامن الكود بين الحاويات
  db-data:
  certbot-etc:
  certbot-var:
